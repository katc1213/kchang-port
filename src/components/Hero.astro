---
import type { HeroProps } from "@types";
type Props = HeroProps;

const { name, specialty, summary, email } = Astro.props;
---

<section class="py-24 md:py-32 flex flex-col items-center text-center" id="#hero">
  <!-- Credit Card Container -->
  <div class="relative w-full max-w-4xl mx-auto mb-16">
    <!-- Credit Card Background -->
    <div class="bg-gradient-to-br from-gray-800 to-gray-900 rounded-3xl p-12 md:p-16 shadow-2xl border border-gray-700">
      <!-- Chip Design (optional decorative element) -->
      <div class="absolute top-8 left-8 w-12 h-10 bg-gradient-to-br from-yellow-400 to-yellow-600 rounded-md opacity-80"></div>
      
      <!-- Card Content -->
      <div class="relative z-10">
        <h1
          class="mb-1.5 font-serif text-5xl md:text-7xl lg:text-8xl font-bold tracking-tightest text-white"
        >
          {name}
        </h1>
        <p
          class="mb-6 font-serif text-2xl md:text-4xl leading-[46px] font-bold tracking-tighter text-primary"
        >
          {specialty}
        </p>
        <p class="text-sm md:text-base font-normal text-gray-300">
          {summary}
        </p>
      </div>
      
      <!-- Decorative elements (optional) -->
      <div class="absolute bottom-8 right-8 flex gap-2">
        <div class="w-10 h-10 rounded-full bg-primary opacity-70"></div>
        <div class="w-10 h-10 rounded-full bg-primary opacity-50"></div>
      </div>
    </div>
  </div>

  <a
    class="inline-block rounded-full bg-primary px-8 py-5 text-sm leading-5 font-medium text-[#fff] hover:bg-primary/90 transition-colors"
    href={`mailto:${email}`}>Get in Touch</a
  >
</section>